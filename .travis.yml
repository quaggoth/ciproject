language: go

go:
  - 1.12.x

# Only clone the most recent commit.
git:
  depth: 1

#install:
  #- Install stuff
 
script:
  - echo Building...
  - go build github.com/quaggoth/ciproject

deploy:
  provider: releases
  #api_key: "GITHUB OAUTH TOKEN"
  file: "ciproject"
  skip_cleanup: true
  on:
    tags: true